# Set the registry to the default npm registry
registry=https://registry.npmjs.org/

# Set the cache directory
cache=~/.npm

# Set the log level
loglevel=warn

# Save exact versions of dependencies
save-exact=true

# Set the package-lock to true
package-lock=true